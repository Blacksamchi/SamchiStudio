/* 읽어들인 정수값이 홀수인가 짝수인가를 판별하는 프로그램 */
#include <studio.h>

int main(void)
{
  int na;
  printf("판별할 정수값을 입력하세요 : "); scanf("%d",&na);

  if (no % 2)
    puts("이 수는 짝수입니다.");
  else
    puts("이 수는 홀수입니다.");
  return(0);
}
/* 2로 나눈 나머지가 0이면 짝수 아니면 홀수인 프로그램임 */
