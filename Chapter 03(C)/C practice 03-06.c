/* 읽어 들인 수가 정수인가 아닌가 */
#include <studio.h>

int main(void)
{
  int num;
  printf("0를 입력해주세요 :") scanf("%d",&num);
  if (num = 0)
    puts("제로를 입력했군요");
  else
    puts("0 이외의 수인데요?");

  return(0);
}
